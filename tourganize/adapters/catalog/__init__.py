"""Adapters of the ``ComponentCatalog`` port.

``yaml`` loads the shipped ``config/catalog/components.yaml``; ``memory`` is the fake every
test builds a catalog with, without touching a file.
"""

from __future__ import annotations
