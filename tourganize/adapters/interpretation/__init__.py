"""``TurnInterpreter`` adapters.

``keyword`` is the deterministic stand-in F05 ships so the state machine can be driven and the
Golden Conversations written without a model. F08 adds the model-backed one behind the same
port, selected by ``TOURGANIZE_INTERPRETER``.
"""

from __future__ import annotations
